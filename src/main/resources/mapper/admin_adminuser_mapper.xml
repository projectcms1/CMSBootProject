<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cms.ca.admin.aduser_repo">

<!-- 관리자 리스트 출력 -->
<select id="admin_list" resultType="com.cms.ca.employee_dto" >
select * from EMP_INFO where MNG_AUTHRT = 'A' order by EMP_NO desc
</select>

<!-- 관리자 리스트 상세보기 출력 -->
<select id="admin_data" resultType="com.cms.ca.employee_dto" parameterType="String">
select a.*, b.ACNT_LCK_YN from EMP_INFO as a JOIN LGN_ACNT_INFO as b where a.USER_NO = b.USER_NO and a.EMP_NO = #{emp_no}
</select>

<!-- 관리자 리스트 검색 기능 -->
<select id="admin_search_list" resultType="com.cms.ca.employee_dto" parameterType="map">
select * from EMP_INFO where ${search_part} like concat('%', #{search_word}, '%') order by EMP_NO desc
</select>

</mapper>